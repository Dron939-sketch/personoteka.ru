# В контейнере сборки домашний каталог недоступен на запись, и npm падает на
# «error writing to the directory: /.npm/_logs». Уводим кэш и логи в /tmp.
cache=/tmp/.npm
logs-dir=/tmp/.npm/_logs

# Лишний шум в логе сборки не нужен.
fund=false
audit=false
update-notifier=false
