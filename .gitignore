node_modules/
.next/
out/
build/
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Рабочий материал для наполнения: тексты-источники, а не контент.
.import/

# Реестр заявок и запросов: ПДн, в репозиторий не попадает.
.data/

# Контактные листы кандидатов на портрет: черновик выбора, а не решение.
# Само решение хранится в content/photo-sources.json — там видно, какой файл
# выбран. Листы весят по полтора мегабайта на персону и устаревают, как только
# на Викисклад загрузят новый снимок.
design/photos/candidates/
